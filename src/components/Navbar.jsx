export default function Navbar() {
  return (
    <header className="bg-sky-100 fixed top-0 w-full z-50 shadow">
      <nav className="max-w-7xl mx-auto flex justify-end gap-6 p-4 text-sm font-medium text-blue-900">
        <a href="#">JOIN MCBS</a>
        <a href="#">EVENTS</a>
        <a href="#">MEET THE TEAM</a>
        <a href="#">CONTACT US</a>
      </nav>
    </header>
  )
}